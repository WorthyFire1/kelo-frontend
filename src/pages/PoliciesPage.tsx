import { useParams } from 'react-router-dom';
import { Breadcrumbs } from '@/components/ui/Breadcrumbs';
import { Container } from '@/components/ui/Container';
import { useDocumentTitle } from '@/hooks/useDocumentTitle';

export function PoliciesPage() {
  const { type } = useParams();
  const isOffer = type === 'offer';
  const title = isOffer ? 'Публичная оферта' : 'Политика конфиденциальности';
  useDocumentTitle(title);

  return (
    <Container className="page-shell legal-page">
      <Breadcrumbs items={[{ label: title }]} />
      <h1>{title}</h1>
      <div className="demo-note">Юридический текст необходимо заменить на согласованную заказчиком редакцию перед публикацией сайта.</div>
      {isOffer ? (
        <>
          <h2>1. Общие положения</h2><p>Настоящий документ является демонстрационной структурой публичной оферты интернет-магазина КЕЛО.</p>
          <h2>2. Предмет договора</h2><p>Продавец обязуется передать товар покупателю, а покупатель — принять и оплатить его на условиях, указанных на сайте.</p>
          <h2>3. Оформление заказа</h2><p>Заказ оформляется через интерфейс сайта. После подключения бэкенда факт создания заказа будет подтверждаться уведомлением.</p>
          <h2>4. Оплата, доставка и возврат</h2><p>Фактические способы оплаты, сроки доставки и правила возврата должны быть утверждены владельцем магазина.</p>
        </>
      ) : (
        <>
          <h2>1. Какие данные обрабатываются</h2><p>Имя, телефон, электронная почта, адрес доставки и сведения, необходимые для оформления заказа.</p>
          <h2>2. Цели обработки</h2><p>Обработка заказов, обратная связь, доставка товаров и улучшение качества сервиса.</p>
          <h2>3. Хранение и защита</h2><p>После подключения бэкенда необходимо описать фактические сроки хранения, место размещения базы данных и меры защиты.</p>
          <h2>4. Права пользователя</h2><p>Пользователь сможет запросить уточнение или удаление своих данных в соответствии с применимым законодательством.</p>
        </>
      )}
    </Container>
  );
}
